import { MultiplyByPipe } from './multiply-by.pipe';

describe('MultiplyByPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyByPipe();
    expect(pipe).toBeTruthy();
  });
});
